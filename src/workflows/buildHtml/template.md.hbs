# Keybindings Reimagined

{{#each data}}
  ## {{{@key}}}

  {{#each this}}
    ### {{{@key}}}

    Keystroke|Command|When
    -|-|-
    {{#each this}}
      {{key}}|{{command}}|{{when}}
    {{/each}}
  {{/each}}
{{/each}}
