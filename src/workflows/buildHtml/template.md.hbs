# Keybindings Reimagined

{{#each data}}
  ## {{{@key}}}

  {{#each this}}
    ### {{{@key}}}

   Keystroke|Command|Condition
   -- |-- |--
    {{#each this}}
      {{key}}|{{command}}|{{when}}
    {{/each}}
  {{/each}}
{{/each}}
