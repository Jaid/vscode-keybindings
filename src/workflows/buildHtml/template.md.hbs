# Keybindings Reimagined

{{#with data}}
  {{#each this}}
    ## {{{this.key}}}
    {{#with this.keystrokes}}
      {{#each this}}
        - {{{this}}}
      {{/each}}
    {{/with}}
  {{/each}}
{{/with}}