# Keybindings Reimagined

{{#each data~}}
  ## {{{startCase @key}}}

  {{#each this~}}
    ### {{{startCase @key}}}

   Keystroke|Command|Condition
   --:|:--|:--
    {{#each this}}
      {{#each visualization ~}}
        {{#if (isModifierKey this)~}}
          `{{title}}`
        {{~else if (isBaseKey this)~}}
          _{{title}}_
        {{~else~}}
          {{title}}
        {{~/if}}
      {{~/each~}}
      |{{breakBeforeDot command}}
      {{~#if when~}}
        |{{{when}}}
      {{~/if}}

    {{/each}}
  {{/each}}
{{/each}}