# Keybindings Reimagined

{{#each data~}}
  ## {{{startCase @key}}}

  {{#each this~}}
    ### {{{startCase @key}}}

   Keystroke|Command|Condition
   --:|:--|:--
    {{#each this}}
      {{#each visualization ~}}
        {{#if (isModifierKey this)~}}
          `{{title}}`
        {{~else if (isBaseKey this)~}}
          _{{title}}_
        {{~else~}}
          {{title}}
        {{~/if}}
      {{~/each~}}
      |{{breakBefore command '.' ' '}}
      {{~#if when~}}
        |{{{breakBefore when '&&' '||'}}}
      {{~/if}}

    {{/each}}
  {{/each}}
{{/each}}