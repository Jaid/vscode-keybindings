# Keybindings Reimagined

{{#each data~}}
  ## {{{startCase @key}}}

  {{#each this~}}
    ### {{{startCase @key}}}

   Keystroke|Command|Condition
    --|-- |--
    {{#each this}}
      {{#each keyVisualization ~}}
        {{#if (isKey type)~}}
          `{{{formatKey value}}}`
        {{~else~}}
          {{{value}}}
        {{~/if}}
      {{~/each~}}
      |`{{command}}`
      {{~#if when~}}
        |`{{when}}`
      {{~/if}}

    {{/each}}
  {{/each}}
{{/each}}