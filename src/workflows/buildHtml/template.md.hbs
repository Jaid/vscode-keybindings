# Keybindings Reimagined

{{#each data~}}
  ## {{{startCase @key}}}

  {{#each this~}}
    ### {{{startCase @key}}}

   Keystroke|Command|Condition
    --|-- |--
    {{#each this}}
      {{#each visualization ~}}
        {{#if (isBaseKey this)~}}
          `{{{formatKey value}}}`
        {{~else if (isModifierKey this)~}}
          _{{{formatKey value}}}_
        {{~else~}}
          {{{value}}}
        {{~/if}}
      {{~/each~}}
      |`{{{command}}}`
      {{~#if when~}}
        |`{{{when}}}`
      {{~/if}}

    {{/each}}
  {{/each}}
{{/each}}