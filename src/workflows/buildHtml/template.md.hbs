# Keybindings Reimagined

{{#each data}}
  ## {{{@key}}}

  {{#each this}}
    ### {{{@key}}}

   Keystroke|Command|Condition
   -- |-- |--
    {{#each this}}
      {{#each keystrokeVisualization}}
        {{{this}}}
      {{/each}}
      |{{command}}
      {{#if condition}}
        |{{condition}}
      {{/if}}
    {{/each}}
  {{/each}}
{{/each}}
